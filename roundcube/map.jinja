{% set server = salt['grains.filter_by']({
    'Debian': {
        'pkgs': ['roundcube-mysql', 'roundcube-plugins', 'roundcube-plugins-extra', 'php5-mcrypt'],
    },
}, merge=salt['pillar.get']('roundcube:server')) %}
